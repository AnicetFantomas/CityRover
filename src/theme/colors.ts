const pallete = {
    white: '#fff',
    black: '#000',
}

export const colors = {
    common: {
        background: pallete.white,
        shadowDefault: pallete.black,
    }
};

export type Colors = typeof colors;